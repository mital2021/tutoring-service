<div class="container row">
    <ol>
        {{#each tutors as |tutor|}}
        <ul>
            <article class="col s12">
                <div class="row">
                    <div class="col s12 m6">
                        <div class="card black darken-1">
                            <div class="card-content white-text home-section">
                                <span class="card-title white-text">{{tutor.first_name}} {{tutor.last_name}}</span>
                                <p>Subject: {{tutor.subject}}</p>
                                <p>Hourly Rate:  ${{tutor.hourly_rate}}</p>
                                <p>email: {{tutor.email}}</p>
                            </div>

                            <div class="card-action">
                                <a class="left" href="/profile/{{tutor.id}}">View Profile</a>
                                <a class="right">({{tutor.reviews.length}}) Review(s)</a>
                                <br>
                            </div>
                        </div>
                    </div>
                </div>
            </article>
        </ul>
        {{/each}}
    </ol>
</div>